# Services
Contains application services and external API wrappers.

- Business logic and Prisma-backed orchestration belong here.
- Keep `server/actions` thin so UI-triggered server entry points stay easy to audit.
